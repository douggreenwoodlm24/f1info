import { SearchDriversPipe } from './search-drivers.pipe';

describe('SearchDriversPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchDriversPipe();
    expect(pipe).toBeTruthy();
  });
});
